@model IEnumerable<Core.Entities.Librarians>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
